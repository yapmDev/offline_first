name: offline_first
description: A generic, reusable offline-first architecture package based on operation-log with incremental sync, operation squashing, and pluggable remote adapters.
version: 0.1.0
homepage: https://github.com/yourusername/offline_first

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  uuid: ^4.0.0
  meta: ^1.9.0
  build: ^2.4.0
  source_gen: ^1.4.0
  analyzer: ^6.0.0

dev_dependencies:
  build_runner: ^2.4.0
  test: ^1.24.0
  lints: ^3.0.0

